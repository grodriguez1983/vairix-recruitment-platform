/**
 * Content-hash helper for the embeddings pipeline (ADR-005).
 * Stub — [GREEN] commit fills it in.
 */
export function contentHash(_model: string, _content: string): string {
  throw new Error('not implemented');
}
